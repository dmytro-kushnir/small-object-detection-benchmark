# EXP-000 — COCO128 smoke baseline (YOLO26n)

**Status:** Concluded (standard entry point for the harness).

**Question:** Does the full pipeline (prepare → train → val infer → `evaluate.py` metrics + viz) run end-to-end?

**What we did:** COCO128, `yolo26n`, 1 epoch, default smoke `imgsz` (320 in the scripted run), no extra filtering. Outputs under `experiments/yolo/test_run/` and `experiments/results/test_run_metrics.json`.

**Conclusion:** Use EXP-000 as the **sanity baseline** before interpreting EXP-001–003. Numbers are **not** publication claims by themselves (tiny data, 1 epoch).

**Paper role:** Background / methods validation only (Table S2: “not in Tables 1–4”).

**Key artifacts:** `experiments/results/test_run_metrics.json`, `experiments/yolo/test_run/predictions_val.json`.

**See also:** [`experiments.md`](../experiments.md#-baseline-exp-000), [`research_analysis.md`](../research_analysis.md#shared-setup-exp-000--exp-001--exp-002--exp-002b--exp-003-as-currently-scripted).
