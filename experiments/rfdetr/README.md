# `experiments/rfdetr/`

RF-DETR training outputs (e.g. EXP-A005 under `ants_expA005/`). Directory is **gitignored** except this README.

Compare to YOLO via `scripts/evaluation/evaluate.py` on the same val COCO GT and `compare_ants_expA005.py`.
