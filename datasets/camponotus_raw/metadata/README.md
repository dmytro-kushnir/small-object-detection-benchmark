# Camponotus Metadata

Keep metadata files that describe data provenance and collection context.

Suggested files:

- `sources.csv` or `sources.json` (source, URL/path, license, date)
- `capture_notes.md` (camera settings, environment notes)
- `sequence_notes.json` (mapping sequence IDs to sessions)

This information supports reproducibility and licensing compliance.
