# Export camponotus YOLO tree → Roboflow RF-DETR COCO layout (train/valid + _annotations.coco.json).

camponotus_yolo_root: datasets/camponotus_yolo
out_root: datasets/camponotus_rfdetr_coco
# "copy" | "hardlink" (hardlink falls back to copy if EXDEV etc.)
copy_mode: copy
