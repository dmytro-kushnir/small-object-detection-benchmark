# @package _global_
# YOLO26 **medium** on full CVAT track_id–majority export @896 (capacity ablation vs yolo26n).

defaults:
  - yolo
  - _self_

model: yolo26m.pt
data: datasets/camponotus_yolo/camponotus_full_export_unique_trackidmajor/dataset.yaml

epochs: 100
imgsz: 896
batch: 4
workers: 4
device: 0

project: experiments/yolo
name: camponotus_trackidmajor_m896

seed: 42
patience: 50
save: true
plots: true
verbose: true
exist_ok: false

optimizer:
  lr0: null
