# @package _global_
# EXP-A000 baseline: ant detector (20 epochs default, override as needed).

defaults:
  - yolo
  - _self_

model: yolo26n.pt
data: datasets/ants_yolo/dataset.yaml
epochs: 20
imgsz: 640
batch: 4
workers: 0
project: experiments/yolo
name: ants_expA000
exist_ok: true
