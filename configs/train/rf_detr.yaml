# Generic RF-DETR placeholder. For **ants EXP-A005**, use:
#   configs/expA005_ants_rfdetr.yaml
#   scripts/train/train_rfdetr_ants.py
# `train_detr.py` forwards to train_rfdetr_ants with that config by default.

dataset_yaml: datasets/processed/example/dataset.yaml
model: null
epochs: 50
batch_size: 8
imgsz: 640
workers: 4
device: null
project: experiments/detr
name: null
seed: 42
